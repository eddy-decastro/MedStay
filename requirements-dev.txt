# Dependances d'EXPLORATION uniquement (EDA, notebooks, figures).
# Volontairement SEPAREES de requirements.txt : ce dernier definit l'image
# Docker de production, qui n'a aucun besoin de jupyter ni de matplotlib
# (les figures de la phase 5 sont generees hors ligne et servies en PNG).
#
# Installation : uv pip install -r requirements.txt -r requirements-dev.txt

matplotlib==3.11.1
seaborn==0.13.2
jupyterlab==4.6.2
nbformat==5.10.4

# Uniquement pour le benchmark comparatif (src/models/benchmark_mlp.py) qui
# justifie le choix de LightGBM. Jamais deploye : garder xgboost hors de
# requirements.txt evite ~200 Mo inutiles dans l'image Render.
xgboost==3.2.0

# MAPIE ne sert qu'a l'ENTRAINEMENT (src/models/calibrate.py). L'artefact
# exporte ne contient que des objets LightGBM : verifie en bloquant l'import de
# mapie, l'API charge et predit sans lui. Le sortir de requirements.txt allege
# l'image Render d'autant.
mapie==0.9.1
